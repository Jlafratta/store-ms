server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:./product-service
    username: sa
    password: sa
  h2:
    console:
      enabled: true
      path: /product-service/h2-console
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    open-in-view: true

mock-data: true